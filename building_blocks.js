// this section of the code will dela with conditionals 
let shoppingDone = false;
let childAllowance;

if (shoppingDone = true) {
    childAllowance = 8090;
}
else {
    childAllowance = 0;
}

// ============ showing the wether for the input selected here ========= //


// working with the cheese example 
const cheese = "Milked";

if (cheese) {
    console.log("there is available cheese in the fridge");
}
else {
    console.log("no available cheese");
}

// =========== returning to the previous example lets test it
let childShopping = false;
let childShoppingAllowance;

if (childShopping) {
    childShoppingAllowance = 980;
    console.log(childShoppingAllowance);
}
else {
    childShoppingAllowance = 10;
    console.log(childShoppingAllowance);
}

// checking the users gender
let currentWether = "hot";
let currentTemprature = 9028;

if (currentWether === "hot" && currentTemprature == 9028) {
    console.log("the wether is pretty bad today");
}
else if (currentWether !== "hot" && currentTemprature === 920) {
    console.log("unexpected values");
}
